services:
  cleaner:
    image: bvdcode/immich-sorter:latest
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      LOCAL_MODE: "true"
      APP_URL: "http://localhost:3000"
    volumes:
      - cleaner-data:/app/data
    restart: unless-stopped
volumes:
  cleaner-data:
